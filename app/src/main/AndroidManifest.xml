<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name=".AppProcessContentProvider"
            android:authorities="esim-cli"
            android:exported="true" />
    </application>

</manifest>

